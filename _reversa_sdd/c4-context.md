# C4 — Context Diagram (Level 1)

> Generated by Reversa Architect · 2026-06-04
> Confidence: 🟢 CONFIRMADO (derived from surface.json, inventory.md, code-analysis.md)

---

## Description

Playwright is a browser automation platform. It serves three primary user types: human developers writing test scripts, AI agents consuming its MCP server as tooling, and CI/CD pipelines running automated test suites. All automation flows through browser processes (Chromium, Firefox, WebKit) launched or connected by Playwright.

---

## Diagram

```mermaid
C4Context
  title Playwright — System Context

  Person(developer, "Developer", "Writes and runs browser automation scripts and tests using the Playwright API or codegen recorder.")
  Person(ai_agent, "AI Agent", "Calls Playwright browser tools via the MCP server (e.g. Claude, Copilot, custom agents).")
  Person(ci_engineer, "CI/CD Pipeline", "Runs playwright test suites in GitHub Actions / Docker environments.")

  System(playwright, "Playwright", "Cross-browser automation platform. Provides: library API, test runner, MCP server, trace viewer, HTML reporter, recorder.")

  System_Ext(chromium, "Chromium Browser", "Google Chrome / Chromium browser process. Controlled via CDP (Chrome DevTools Protocol).")
  System_Ext(firefox, "Firefox Browser", "Mozilla Firefox browser process. Controlled via Juggler custom protocol.")
  System_Ext(webkit, "WebKit Browser", "Apple WebKit browser process. Controlled via custom WebKit protocol.")
  System_Ext(android, "Android Device", "Android device or emulator. Controlled via ADB socket.")
  System_Ext(electron_app, "Electron App", "Target Electron application under test. Controlled via Electron IPC.")
  System_Ext(npm_registry, "npm Registry", "Package distribution. Publishes playwright, @playwright/test, playwright-core.")
  System_Ext(docker_hub, "Docker Hub", "Container registry. Publishes mcr.microsoft.com/playwright images.")
  System_Ext(github_actions, "GitHub Actions", "CI/CD platform running 21 test + release workflows.")
  System_Ext(chrome_extension, "Chrome Extension", "Playwright browser extension that relays CDP to MCP server via Extension Protocol v2.")

  Rel(developer, playwright, "Writes test scripts / runs codegen", "Node.js API / CLI")
  Rel(ai_agent, playwright, "Calls browser tools", "MCP (stdio / TCP JSON-RPC)")
  Rel(ci_engineer, playwright, "Runs test suites", "CLI / GitHub Actions")

  Rel(playwright, chromium, "Launches + controls", "CDP over WebSocket")
  Rel(playwright, firefox, "Launches + controls", "Juggler over WebSocket")
  Rel(playwright, webkit, "Launches + controls", "WebKit protocol over WebSocket")
  Rel(playwright, android, "Controls device", "ADB socket")
  Rel(playwright, electron_app, "Attaches + controls", "Electron IPC")
  Rel(playwright, chrome_extension, "Relays CDP", "Extension Protocol v2 / WebSocket")

  Rel(playwright, npm_registry, "Publishes packages", "HTTPS")
  Rel(playwright, docker_hub, "Publishes images", "HTTPS")
  Rel(github_actions, playwright, "Runs CI workflows", "GitHub Actions runner")
```

---

## External Systems Summary

| System | Type | Protocol | Direction |
|--------|------|----------|-----------|
| Chromium | Browser engine | CDP / WebSocket | Bidirectional |
| Firefox | Browser engine | Juggler / WebSocket | Bidirectional |
| WebKit | Browser engine | Custom / WebSocket | Bidirectional |
| Android Device | Mobile device | ADB socket | Bidirectional |
| Electron App | Desktop app | Electron IPC | Bidirectional |
| Chrome Extension | Browser extension | Extension Protocol v2 | Bidirectional |
| npm Registry | Package registry | HTTPS | Outbound |
| Docker Hub | Container registry | HTTPS | Outbound |
| GitHub Actions | CI/CD | GitHub Actions YAML | Inbound |
| AI Agents (MCP clients) | AI tooling | MCP stdio/TCP | Inbound |
