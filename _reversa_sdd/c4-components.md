# C4 — Components Diagram (Level 3)

> Generated by Reversa Architect · 2026-06-04
> Focus containers: playwright-core (Client Layer + Server Layer + Dispatcher Layer), MCP Server
> Confidence: 🟢 CONFIRMADO (derived from code-analysis.md, modules.json)

---

## playwright-core — Client Layer Components

```mermaid
C4Component
  title playwright-core — Client Layer Components

  Container_Boundary(client_layer, "Client Layer (playwright-core/src/client)") {

    Component(channel_owner, "ChannelOwner<T>", "TypeScript abstract class", "Abstract base for all public API objects. Wraps a Proxy channel. Tracks children, handles GC disposal, routes every call through _wrapApiCall with ApiZone tracing.")

    Component(connection, "Connection", "TypeScript class", "Manages the WebSocket/pipe transport. Maintains GUID → ChannelOwner registry. Routes incoming server events to the correct object. Sends serialized messages to server.")

    Component(page, "Page", "TypeScript class extends ChannelOwner", "Top-level page object. Owns Frame tree, route handlers, locator handlers. Provides click/fill/navigate/screenshot/evaluate APIs.")

    Component(frame, "Frame", "TypeScript class extends ChannelOwner", "Browsing frame (main or iframe). Owns locator queries, JS evaluation, waiting for selectors/navigation.")

    Component(locator, "Locator", "TypeScript class", "Lazy, auto-retrying element reference. Does not hold a live DOM reference. Enforces frame boundary on has/hasNot filters.")

    Component(browser_context, "BrowserContext", "TypeScript class extends ChannelOwner", "Isolated browser session — cookies, localStorage, permissions, service workers, routes.")

    Component(browser, "Browser", "TypeScript class extends ChannelOwner", "Browser process handle. Creates contexts, manages CDP sessions.")

    Component(browser_type, "BrowserType", "TypeScript class extends ChannelOwner", "Entry point for launch/connect. Resolves chromium/firefox/webkit.")

    Component(network, "Network (Request/Response/Route/WebSocket)", "TypeScript classes extends ChannelOwner", "Network interception. Route can fulfill/abort/continue requests. WebSocketRoute for WS interception.")

    Component(waiter, "Waiter", "TypeScript class", "Internal waitForEvent helper. Manages timeout, abort signal, and event listener lifecycle.")

    Component(api_request_context, "APIRequestContext", "TypeScript class extends ChannelOwner", "page.request / playwright.request — direct HTTP calls outside browser.")
  }

  Rel(page, channel_owner, "extends")
  Rel(frame, channel_owner, "extends")
  Rel(browser_context, channel_owner, "extends")
  Rel(browser, channel_owner, "extends")
  Rel(browser_type, channel_owner, "extends")
  Rel(network, channel_owner, "extends")
  Rel(api_request_context, channel_owner, "extends")

  Rel(channel_owner, connection, "sends messages via", "Proxy RPC")
  Rel(page, frame, "owns main frame + iframe tree")
  Rel(frame, locator, "creates")
  Rel(browser_context, page, "creates")
  Rel(browser, browser_context, "creates")
  Rel(browser_type, browser, "launches / connects")
  Rel(page, network, "routes requests through")
  Rel(page, waiter, "uses for waitForEvent")
```

---

## playwright-core — Server Layer Components

```mermaid
C4Component
  title playwright-core — Server Layer Components

  Container_Boundary(server_layer, "Server Layer (playwright-core/src/server)") {

    Component(sdk_object, "SdkObject", "TypeScript abstract class", "Base for all server objects. Carries guid (prefix@uuid), attribution chain, and shared Instrumentation instance.")

    Component(instrumentation, "Instrumentation", "TypeScript class", "Shared hook system — onBeforeCall, onAfterCall, onBrowserClose, etc. Used by test runner and tracer for recording.")

    Component(playwright_root, "Playwright (root)", "TypeScript class extends SdkObject", "Root object. Owns BrowserType instances. Created once per process.")

    Component(server_browser_type, "BrowserType (server)", "TypeScript class extends SdkObject", "Spawns/connects browser processes. Has browser-specific implementations in chromium/, firefox/, webkit/.")

    Component(server_browser, "Browser (server)", "TypeScript class extends SdkObject", "Browser process lifecycle. Creates BrowserContexts.")

    Component(server_context, "BrowserContext (server)", "TypeScript class extends SdkObject", "Isolated context — cookies, network, permissions, service workers. Owns Pages.")

    Component(server_page, "Page (server)", "TypeScript class extends SdkObject", "Page lifecycle: navigation, input, frame tree, screenshots, video recording.")

    Component(server_frame, "Frame (server)", "TypeScript class extends SdkObject", "Frame evaluation, selector resolution, navigation, waiter management.")

    Component(selector_engine, "SelectorEngine / Selectors", "TypeScript class", "Selector parsing + resolution pipeline. Dispatches to CSS, XPath, ARIA, role, text, and custom engines.")

    Component(progress_controller, "ProgressController", "TypeScript class", "Timeout-aware async operation tracking. Wraps operations to enforce deadlines, propagate cancellation.")

    Component(network_server, "Network (server)", "TypeScript classes", "Request, Response, Route server-side objects. Interception, HAR recording, routing.")
  }

  Rel(server_browser_type, sdk_object, "extends")
  Rel(server_browser, sdk_object, "extends")
  Rel(server_context, sdk_object, "extends")
  Rel(server_page, sdk_object, "extends")
  Rel(server_frame, sdk_object, "extends")

  Rel(sdk_object, instrumentation, "carries reference to")
  Rel(playwright_root, server_browser_type, "owns 3 (chromium/firefox/webkit)")
  Rel(server_browser_type, server_browser, "launches")
  Rel(server_browser, server_context, "creates")
  Rel(server_context, server_page, "creates")
  Rel(server_page, server_frame, "owns main frame + iframes")
  Rel(server_frame, selector_engine, "resolves selectors via")
  Rel(server_page, progress_controller, "uses for timeout-aware ops")
  Rel(server_context, network_server, "routes requests through")
```

---

## playwright-core — Dispatcher Layer Components

```mermaid
C4Component
  title playwright-core — Dispatcher Layer Components

  Container_Boundary(dispatcher_layer, "Dispatcher Layer (playwright-core/src/server/dispatchers)") {

    Component(dispatcher_base, "Dispatcher<Type,Channel,Parent>", "TypeScript abstract class", "Base RPC bridge. Wraps one SdkObject. Registered in DispatcherConnection by GUID. Receives protocol messages, calls server methods, emits events to client. Manages GC bucket membership.")

    Component(dispatcher_connection, "DispatcherConnection", "TypeScript class", "Owns all dispatchers. Routes incoming messages to correct Dispatcher by GUID. Sends events back to client transport.")

    Component(page_dispatcher, "PageDispatcher", "TypeScript class extends Dispatcher", "Bridges client Page ↔ server Page. Handles click, fill, screenshot, evaluate, route commands.")

    Component(frame_dispatcher, "FrameDispatcher", "TypeScript class extends Dispatcher", "Bridges client Frame ↔ server Frame.")

    Component(context_dispatcher, "BrowserContextDispatcher", "TypeScript class extends Dispatcher", "Bridges client BrowserContext ↔ server BrowserContext.")

    Component(browser_dispatcher, "BrowserDispatcher", "TypeScript class extends Dispatcher", "Bridges client Browser ↔ server Browser.")

    Component(browser_type_dispatcher, "BrowserTypeDispatcher", "TypeScript class extends Dispatcher", "Bridges client BrowserType ↔ server BrowserType. Root entry for launch/connect.")

    Component(element_dispatcher, "ElementHandlerDispatcher", "TypeScript class extends Dispatcher", "Bridges ElementHandle. GC bucket limit: 100,000.")
  }

  Rel(page_dispatcher, dispatcher_base, "extends")
  Rel(frame_dispatcher, dispatcher_base, "extends")
  Rel(context_dispatcher, dispatcher_base, "extends")
  Rel(browser_dispatcher, dispatcher_base, "extends")
  Rel(browser_type_dispatcher, dispatcher_base, "extends")
  Rel(element_dispatcher, dispatcher_base, "extends")

  Rel(dispatcher_base, dispatcher_connection, "registered in")
  Rel(dispatcher_connection, dispatcher_base, "routes messages to")
```

---

## MCP Server Components

```mermaid
C4Component
  title MCP Server Components (playwright-core/src/tools/mcp)

  Container_Boundary(mcp_container, "MCP Server") {

    Component(mcp_entry, "mcp.ts (entry)", "TypeScript / Node.js", "Process entry point. Parses CLI args, resolves config, starts MCP server.")

    Component(mcp_config, "config.ts", "TypeScript module", "5-layer config merge: defaults → global config file → project config file → env vars → CLI flags. Validates output dir, resolves browser name, applies platform headless defaults.")

    Component(browser_factory, "browserFactory.ts", "TypeScript class", "ClientFactory registry — manages browser connections per session. Supports: launch new, connect CDP, connect remote Playwright server.")

    Component(browser_model, "browserModel.ts", "TypeScript class", "Chrome extension CDP relay. Manages known tabs and attached TabSessions. Extension Protocol v2 handshake.")

    Component(mcp_program, "program.ts", "TypeScript module", "Tool definitions — defines all AI-callable browser tools using defineTool() / defineTabTool(). Groups: core, vision, pdf, devtools, etc.")

    Component(mcp_index, "index.ts", "TypeScript module", "MCP server lifecycle — starts listening (stdio or TCP), registers tools, handles session creation/teardown.")
  }

  Rel(mcp_entry, mcp_config, "resolves config via")
  Rel(mcp_entry, mcp_index, "starts server via")
  Rel(mcp_index, mcp_program, "registers tools from")
  Rel(mcp_index, browser_factory, "creates browser sessions via")
  Rel(browser_factory, browser_model, "uses for extension relay")
  Rel(mcp_config, browser_factory, "configures")
```

---

## Injected Scripts Components

```mermaid
C4Component
  title Injected Scripts Components (packages/injected/src)

  Container_Boundary(injected_bundle, "Injected Scripts (browser page context)") {

    Component(injected_script, "injectedScript.ts", "TypeScript (browser bundle)", "Main entry — all injected APIs exposed on the window binding. Orchestrates selector engine, ARIA, highlight, recorder.")

    Component(selector_engine_inj, "selectorEngine.ts + selectorEvaluator.ts", "TypeScript (browser bundle)", "Selector dispatch — routes to CSS, XPath, role, text, ARIA, layout engines. Evaluates selectors against live DOM.")

    Component(selector_generator, "selectorGenerator.ts", "TypeScript (browser bundle)", "Generates stable selectors for DOM elements. Priority: test-id → ARIA role → text → CSS. Used by Recorder.")

    Component(aria_snapshot, "ariaSnapshot.ts", "TypeScript (browser bundle)", "Walks accessibility tree to produce structured ARIA snapshot. Powers toMatchAriaSnapshot() assertions.")

    Component(highlight, "highlight.ts", "TypeScript (browser bundle)", "Element highlight overlay for the Inspector and Recorder. Renders colored border + label.")

    Component(clock_inj, "clock.ts", "TypeScript (browser bundle)", "Fake clock injection — patches Date, setTimeout, setInterval, requestAnimationFrame for time control.")

    Component(bindings_ctrl, "bindingsController.ts", "TypeScript (browser bundle)", "Manages exposeFunction bindings — registers callbacks from Node.js as browser-callable functions.")

    Component(recorder_inj, "recorder/ (injected)", "TypeScript (browser bundle)", "Detects user actions (click, fill, select, navigate) during codegen sessions. Reports to Recorder UI via binding.")
  }

  Rel(injected_script, selector_engine_inj, "dispatches selectors to")
  Rel(injected_script, aria_snapshot, "uses for ARIA assertions")
  Rel(injected_script, highlight, "uses for Inspector overlay")
  Rel(injected_script, clock_inj, "uses for time control")
  Rel(injected_script, bindings_ctrl, "uses for exposeFunction")
  Rel(recorder_inj, selector_generator, "uses to generate selectors")
```
