# Flowchart — playwright-core/client

> Generated by Reversa Archaeologist · 2026-06-04
> 🟢 CONFIRMADO — extracted from channelOwner.ts

---

## API Call Flow (ChannelOwner._wrapApiCall)

```mermaid
flowchart TD
    A[User calls page.click / locator.fill / etc.] --> B{Existing ApiZone in current zone?}
    B -- yes --> C[Execute func in existing zone\nno tracing overhead]
    B -- no --> D[captureLibraryStackTrace\nget apiName + frames]
    D --> E[Create ApiZone\napiName, frames, internal=false]
    E --> F[Push zone to platform.zones.current]
    F --> G[instrumentation.onApiCallBegin\nlogging + tracing starts]
    G --> H[sendMessageToServer\nJSON RPC to server via WebSocket]
    H --> I{Success?}
    I -- yes --> J[instrumentation.onApiCallEnd\nlogging + tracing ends]
    J --> K[Return result to user]
    I -- no --> L[Fix error stack\nPrepend apiName to message\nAppend user stack frames]
    L --> M[apiZone.error = e\ninstrumentation.onApiCallEnd with error]
    M --> N[throw error]
```

---

## Channel Proxy Flow (_createChannel)

```mermaid
flowchart TD
    A[channel.someMethod params] --> B{Is string prop?}
    B -- no --> C[Return base object property]
    B -- yes --> D[maybeFindValidator type, prop, Params]
    D --> E{Validator found?}
    E -- no --> C
    E -- yes --> F[_wrapApiCall]
    F --> G[validator params — validate + serialize]
    G --> H{apiZone.internal or already reported?}
    H -- no --> I[Mark reported=true\ninstrumentation.onApiCallBegin\nsendMessageToServer with apiZone]
    H -- yes --> J[sendMessageToServer internal=true\nno tracing]
```

---

## Object Lifecycle

```mermaid
flowchart TD
    A[Server creates SdkObject] --> B[Dispatcher registers guid]
    B --> C[DispatcherConnection sends initializer to client]
    C --> D[Connection._objects.set guid, channelOwner]
    D --> E[parent._objects.set guid, channelOwner]
    E --> F[Object is live and usable]
    F --> G{How disposed?}
    G -- user calls close --> H[_dispose reason=undefined\n_wasCollected = false]
    G -- GC bucket overflow --> I[_dispose reason=gc\n_wasCollected = true]
    H --> J[Remove from parent._objects]
    I --> J
    J --> K[Remove from connection._objects]
    K --> L[Recursively dispose children]
```

---

## Event Subscription Management

```mermaid
flowchart TD
    A[user: page.on event, listener] --> B{listenerCount event == 0 before add?}
    B -- yes --> C[_updateSubscription event, enabled=true\nprotocol: updateSubscription to server]
    B -- no --> D[skip protocol call\nserver already subscribed]
    C --> E[super.on event, listener]
    D --> E
    E --> F[Listener registered]

    G[user: page.off event, listener] --> H[super.off event, listener]
    H --> I{listenerCount event == 0 after remove?}
    I -- yes --> J[_updateSubscription event, enabled=false\nprotocol: server stops sending event]
    I -- no --> K[skip — other listeners still active]
```
