# Flowchart — playwright-core/tools/mcp

> Generated by Reversa Archaeologist · 2026-06-04
> 🟢 CONFIRMADO — extracted from config.ts, browserModel.ts

---

## MCP Config Resolution (resolveCLIConfigForMCP)

```mermaid
flowchart TD
    A[resolveCLIConfigForMCP cliOptions, env] --> B[configFromEnv env\nread PLAYWRIGHT_MCP_* vars]
    B --> C[configFromCLIOptions cliOptions\nmap CLI flags to Config]
    C --> D[Determine configFile\nCLI flag overrides env var]
    D --> E[loadConfig configFile\nJSON or INI parse]
    E --> F[Start with defaultConfig\naction=5s nav=60s expect=5s]
    F --> G[mergeConfig default + resolveConfigPaths fileConfig]
    G --> H[mergeConfig + resolveConfigPaths envOverrides]
    H --> I[mergeConfig + resolveConfigPaths cliOverrides]
    I --> J[validateBrowserConfig\nset browserName, sandbox, viewport]
    J --> K{headless undefined?}
    K -- yes on linux --> L[headless = !DISPLAY env var]
    K -- no --> M[keep user value]
    L --> N[validateOutputDir]
    M --> N
    N --> O[Return FullConfig]
```

---

## Browser Name Resolution

```mermaid
flowchart TD
    A[--browser option] --> B{value?}
    B -- chrome / msedge / *-beta etc --> C[browserName=chromium\nchannel=input value]
    B -- chromium --> D[browserName=chromium\nchannel=chrome-for-testing]
    B -- firefox --> E[browserName=firefox\nchannel=none]
    B -- webkit --> F[browserName=webkit\nchannel=none]
    B -- undefined --> G[browserName=chromium\nchannel=chrome]
```

---

## BrowserModel Extension Lifecycle (CDP Relay)

```mermaid
flowchart TD
    A[Chrome Extension connects] --> B[Extension pushes chrome.tabs.onCreated\nfor each existing tab]
    B --> C[Model records tabs in _knownTabs\nnot yet attached]
    C --> D[Extension sends extension.initialized]
    D --> E[ready resolves\nRelay unpauses Playwright WebSocket]
    E --> F[Playwright sends Target.setAutoAttach]
    F --> G[model.enableAutoAttach called]
    G --> H[Attach debugger to each _knownTabs entry\nAssign sessionId, move to _tabSessions]
    H --> I[Emit Target.attachedToTarget\nfor each tab to Playwright client]
    I --> J[Live: new tabs created]
    J --> K[chrome.tabs.onCreated → model.onTabCreated\nIf autoAttach: attach + emit immediately]

    L[Playwright CDP client connects] --> M[connectOverCDP wires _sendToCDPClient]
    M --> N[Model can now emit CDP events to Playwright]
```
