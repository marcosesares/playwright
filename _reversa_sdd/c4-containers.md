# C4 — Containers Diagram (Level 2)

> Generated by Reversa Architect · 2026-06-04
> Confidence: 🟢 CONFIRMADO (derived from surface.json, inventory.md, code-analysis.md, domain.md)

---

## Description

Playwright's runtime containers are primarily Node.js processes. The core engine (`playwright-core`) implements both client and server in the same npm package; they communicate through an in-process or network RPC channel. The tooling UIs (html-reporter, trace-viewer, recorder) are standalone React SPAs served statically or embedded in the CLI. The MCP server runs as a separate entry-point process exposing browser automation over the Model Context Protocol.

---

## Diagram

```mermaid
C4Container
  title Playwright — Containers

  Person(developer, "Developer", "Writes tests / uses CLI")
  Person(ai_agent, "AI Agent", "Calls MCP tools")

  System_Boundary(playwright_system, "Playwright") {

    Container(pw_client, "Client Layer", "TypeScript / Node.js", "Public API — ChannelOwner hierarchy (Page, Frame, Locator, BrowserContext, Browser, BrowserType, etc.). Routes all calls through the RPC protocol to the server layer.")

    Container(protocol_spec, "Protocol (YAML Spec)", "YAML DSL + codegen", "Contract-first RPC spec: 19 YAML files define all channels, commands, events. Generated TypeScript types (channels.d.ts) and validators used by both client and server.")

    Container(pw_server, "Server Layer", "TypeScript / Node.js", "SdkObject hierarchy — actual browser control. Spawns / connects browser processes. Manages BrowserContext, Page, Frame, Route, Network lifecycle.")

    Container(dispatchers, "Dispatcher Layer", "TypeScript / Node.js", "RPC bridge. Wraps every SdkObject in a Dispatcher. Receives protocol messages from client, calls server methods, emits server events back to client. GC bucket eviction for handle limits.")

    Container(mcp_server, "MCP Server", "TypeScript / Node.js", "Exposes browser automation as AI-callable tools over the Model Context Protocol (stdio or TCP). Layered config: defaults → global config → project config → env vars → CLI flags.")

    Container(playwright_test, "Test Runner (@playwright/test)", "TypeScript / Node.js", "Fixture system, test lifecycle, retries, parallelism, reporters. Self-hosted — runs on itself. Wraps playwright-core.")

    Container(html_reporter, "HTML Reporter", "React 19 / Vite SPA", "Displays test run results: file tree, Gantt chart, test detail, error view, filtering, labels. Loads report JSON data.")

    Container(trace_viewer, "Trace Viewer", "React 19 / Vite SPA + Service Worker", "Replays .zip trace files: network, actions, DOM snapshots, screenshots, console. Service worker serves snapshot HTML offline.")

    Container(recorder, "Recorder UI", "React 19 / Vite SPA", "Overlay UI during codegen sessions. Displays recorded actions, allows editing selectors, generates test code.")

    Container(injected, "Injected Scripts", "TypeScript (browser bundle)", "Bundled scripts injected into browser page context at runtime. Provides: selector engine, ARIA snapshot, element highlight, fake clock, recorder action detection, bindings controller.")

    Container(cli_daemon, "CLI Daemon", "TypeScript / Node.js", "Main CLI process entry. Handles: install, codegen, show-report, show-trace, open, screenshot commands.")

    Container(extension, "Chrome Extension", "Browser Extension (MV3)", "Relays CDP between Chrome browser tabs and the Playwright MCP server via Extension Protocol v2.")
  }

  System_Ext(chromium, "Chromium Process", "Browser engine — CDP")
  System_Ext(firefox, "Firefox Process", "Browser engine — Juggler")
  System_Ext(webkit, "WebKit Process", "Browser engine — custom")

  Rel(developer, pw_client, "Uses API", "Node.js import")
  Rel(developer, playwright_test, "Runs tests", "CLI / npx playwright test")
  Rel(developer, cli_daemon, "Runs commands", "CLI / npx playwright")
  Rel(ai_agent, mcp_server, "Calls tools", "MCP stdio / TCP")

  Rel(pw_client, protocol_spec, "Serializes calls using generated types", "In-process")
  Rel(pw_client, dispatchers, "Sends RPC messages", "WebSocket / in-process pipe")
  Rel(dispatchers, protocol_spec, "Uses generated validators", "In-process")
  Rel(dispatchers, pw_server, "Calls server methods", "In-process")
  Rel(pw_server, injected, "Injects scripts at runtime", "CDP evaluateOnNewDocument")
  Rel(pw_server, chromium, "Controls", "CDP / WebSocket")
  Rel(pw_server, firefox, "Controls", "Juggler / WebSocket")
  Rel(pw_server, webkit, "Controls", "WebKit protocol / WebSocket")

  Rel(playwright_test, pw_client, "Uses automation API", "Node.js import")
  Rel(mcp_server, pw_client, "Uses automation API", "Node.js import")
  Rel(mcp_server, extension, "Relays CDP", "Extension Protocol v2")
  Rel(cli_daemon, pw_client, "Drives browser", "Node.js import")
  Rel(cli_daemon, html_reporter, "Serves via HTTP", "Static file serve")
  Rel(cli_daemon, trace_viewer, "Serves via HTTP", "Static file serve")
```

---

## Container Summary

| Container | Technology | Deployment | Purpose |
|-----------|-----------|------------|---------|
| Client Layer | TypeScript / Node.js | In-process | Public API (ChannelOwner) |
| Protocol Spec | YAML + codegen | Build-time | Source of truth for RPC contracts |
| Server Layer | TypeScript / Node.js | In-process or separate process | Browser control (SdkObject) |
| Dispatcher Layer | TypeScript / Node.js | In-process | RPC bridge between client and server |
| MCP Server | TypeScript / Node.js | Standalone process | AI agent browser tooling |
| Test Runner | TypeScript / Node.js | Standalone process | @playwright/test |
| HTML Reporter | React 19 SPA | Browser (static) | Test result visualization |
| Trace Viewer | React 19 SPA + SW | Browser (static) | Trace file replay |
| Recorder UI | React 19 SPA | Browser overlay | Codegen recording |
| Injected Scripts | TypeScript (browser bundle) | Browser page context | Selector engine, highlights, clock |
| CLI Daemon | TypeScript / Node.js | CLI process | Command-line interface |
| Chrome Extension | Browser Extension MV3 | Chrome browser | CDP relay for MCP mode |
